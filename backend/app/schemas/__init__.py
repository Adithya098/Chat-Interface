"""Pydantic schema package for backend request and response contracts.

This package contains typed payload models used by routers to validate incoming data 
and serialize outgoing API responses consistently across users, rooms, memberships, and messages."""
