"""Router package marker for HTTP and WebSocket endpoint modules.

This package groups endpoint modules by domain (users, rooms, members, messages, files, realtime websocket),
which are imported and mounted by the main FastAPI application."""
